# Optimised Face Detection & Recognition using Yunet & AWS

### Steps to run the code

- Configure AWS CLI
- (Recommended) Create a virtual environment
- Install the dependencies using `pip install -r requirements.txt`
- Create a AWS collection
- Execute the code using `python3 main.py`
